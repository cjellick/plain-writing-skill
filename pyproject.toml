[project]
name = "plain-writing-evals"
version = "0.1.0"
description = "Before/after evals for the plain-writing skill"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "openai>=1.60.0",
  "python-dotenv>=1.0.0",
  "datasets>=3.0.0",
]

[tool.uv]
package = false
